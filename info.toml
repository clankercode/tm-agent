[meta]
name     = "TM Agent"
author   = "XertroV"
category = "Agent"
version  = "0.1.0"

[script]
dependencies = ["mcp-tm", "ai-api", "MLHook"]
export_dependencies = ["MLHook"]

module = "Agent"
timeout = 60000
